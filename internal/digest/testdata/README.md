# Digest test fixtures

The JSONL files in this directory are hand-authored synthetic streams. They
use the filenames emitted by the supported agent CLIs, but no event, message,
identifier, path, code sample, or usage value was copied from a real run.

Keep these fixtures small and invented. Prefer focused inline fixtures for new
edge cases instead of committing captured agent sessions.
